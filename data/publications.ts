// Shape of each publication object:
// {
//   id: string;           — unique slug or DOI
//   citation: string;     — full APA-style citation string
//   year: number;
//   pdfUrl?: string;      — link to PDF if available
// }

export const publications: never[] = [];
