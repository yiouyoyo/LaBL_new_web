// Shape of each project object:
// {
//   id: string;           — unique slug, e.g. "pronet"
//   name: string;         — display name, e.g. "ProNET"
//   description: string;  — 2–3 sentence summary
//   funder?: string;      — funding body, e.g. "NIMH"
//   logo?: string;        — path under /public/images/
//   detailUrl?: string;   — external link or internal route
// }

export const projects: never[] = [];
