// Shape of each person object:
// {
//   id: string;           — unique slug
//   name: string;
//   photo?: string;       — path under /public/images/
//   focusArea: string;    — 1-line research focus
//   status: "current" | "alumni";
// }
// Moving someone from current → alumni is a one-field change.

export const people: never[] = [];
